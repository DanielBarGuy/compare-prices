# DATA-SOURCES.md — מקורות נתוני מחירים ומבצעים

**סטטוס:** טיוטת מחקר חלקית. נכתב 2026-09-10 על ידי Claude (משימת מחקר בלבד).
**חשוב:** המסמך מבחין במפורש בין מה שאומת בהרצה הזו לבין מה שמבוסס על ידע כללי וטרם אומת. אין במסמך שום מחיר, מבצע או נתון מוצר — לא בוצעה הורדת קבצים בפועל.

---

## 1. רקע רגולטורי

חובת הפרסום נובעת מ**חוק קידום התחרות בענף המזון, התשע"ד-2014** ומ**תקנות קידום התחרות בענף המזון (שקיפות מחירים), התשע"ה-2014**. הרגולציה מחייבת קמעונאים גדולים לפרסם באופן פומבי, ללא תשלום, קבצי מחירים ומבצעים לכל סניף. זהו הבסיס החוקי לכך שהגישה לנתונים אינה דורשת עקיפת חסימות — הנתונים אמורים להיות נגישים לציבור.

מקורות רקע (לא נבדקו טכנית, לעיון בלבד):
- לשכת המסחר — נוסח התקנות: https://www.chamber.org.il/sectors/1187/1197/13651/13653/29904/
- עמוד הרגולציה באתר gov.il (ראו §3 — לא נגיש ל-curl בהרצה זו)

---

## 2. תוצאות בדיקה בפועל (2026-09-10)

הפקודה שהורצה (בדיקת זמינות בלבד, ללא הורדת תוכן):

```bash
curl -s -o /dev/null -w "%{http_code}" -L --max-time 25 -A "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)" "<URL>"
```

| כתובת | קוד תגובה | פרשנות |
|---|---|---|
| `https://prices.shufersal.co.il/` | **200** | הפורטל חי ומגיב ללא אימות |
| `https://url.publishedprices.co.il/login` | **200** | פורטל Cerberus חי; דורש התחברות |
| `https://laibcatalog.co.il/` | **200** | פורטל חי |
| `https://www.gov.il/he/pages/cpfta_prices_regulations` | **403** | חסימת בוטים של gov.il מול curl. סביר שנגיש מדפדפן — **טרם אומת** |
| `https://url.retail.publishedprices.co.il/login` | **000** | כשל חיבור/DNS מהמכונה הזו. הסיבה **לא אובחנה** (הבדיקה המאבחנת לא הורצה) |
| `http://matrixcatalog.co.il/NBCompetitionRegulations.aspx` | **000** | כשל חיבור. הסיבה **לא אובחנה**; ייתכן HTTPS/סאב-דומיין שונה |

`000` ב-curl = לא התקבלה תגובת HTTP כלל (DNS, TCP, או TLS). זה **אינו** בהכרח חסימה — יש לחזור ולאבחן.

---

## 3. מיפוי רשתות לפורטלים

⚠️ **אזהרה:** ההצמדה של רשת לפורטל בטבלה הבאה מבוססת על ידע כללי ועל תוצאות חיפוש, ו**טרם אומתה מול הפורטל עצמו בהרצה הזו**. יש לאמת כל שורה לפני כתיבת קוד שמסתמך עליה.

| רשת | פורטל משוער | סטטוס אימות |
|---|---|---|
| **שופרסל** | `https://prices.shufersal.co.il/` — פורטל עצמאי, ללא התחברות | פורטל אומת חי (200). ההצמדה לשופרסל ודאית מהדומיין עצמו |
| **רמי לוי** | Cerberus — `https://url.publishedprices.co.il/login` | הפורטל אומת חי (200). שם המשתמש והתיקייה **לא אומתו** |
| **יוחננוף** | Cerberus — אותו פורטל | הפורטל אומת חי. ההצמדה **לא אומתה** |
| **קרפור** (מופעלת ע"י אלקטרה צריכה) | ככל הנראה Cerberus | **לא אומת כלל** — נקודת המחקר החלשה ביותר |
| **ויקטורי** | `matrixcatalog.co.il` (פלטפורמת Matrix) | הכתובת לא נענתה בבדיקה. ההצמדה **לא אומתה** |

**מוסכמת גישה ידועה ל-Cerberus (טרם אומתה):** הפורטל מיועד לגישה ציבורית לפי התקנות; לכל רשת שם משתמש ייעודי (למשל בסגנון `RamiLevi`, `yohananof`) עם סיסמה ריקה או ידועה שהרשת מפרסמת. **אין ליצור חשבון חדש** ואין לנחש סיסמאות — יש לאתר את הפרטים שהרשת מפרסמת בעצמה, בדף "שקיפות מחירים" באתר הרשמי של כל רשת. זהו הצעד הבא הקריטי.

---

## 4. סוגי הקבצים — PriceFull / Price / PromoFull / Promo

⚠️ כל §4 מבוסס על התקנות ועל מוסכמות מקובלות בתעשייה — **טרם אומת מול קובץ אמיתי שהורד**.

| סוג | תוכן | שימוש אצלנו |
|---|---|---|
| `PriceFull` | תמונת מצב מלאה של כל המוצרים בסניף | טעינת בסיס (baseline) |
| `Price` | דלתא — רק מה שהשתנה מאז הקובץ המלא | עדכון תדיר וזול |
| `PromoFull` | כל המבצעים הפעילים בסניף | טעינת בסיס למבצעים |
| `Promo` | דלתא של מבצעים | עדכון תדיר |
| `Stores` | רשימת סניפים של הרשת (מזהה, שם, עיר, כתובת) | טבלת סניפים |

**מוסכמת שמות קבצים (לאימות):** `PriceFull<ChainId>-<StoreId>-<YYYYMMDDHHmm>.gz` — קובץ GZIP שבתוכו XML. `ChainId` הוא מזהה בן 13 ספרות לרשת. הנחה זו חייבת להיבדק מול רשימת קבצים אמיתית לפני מימוש parser.

### שדות שצריך לחלץ (לפי דרישת המשימה)

**מוצר / ברקוד:** `ItemCode` (ברקוד — לרוב EAN-13), `ItemName`, `ItemPrice`, `UnitQty`, `Quantity`, `UnitOfMeasurePrice` (מחיר ליחידת מידה — קריטי להשוואה הוגנת), `ItemType` (מבחין בין מוצר עם ברקוד גלובלי לבין מוצר פנימי של הרשת — **חשוב**: קוד פנימי אינו בר-השוואה בין רשתות).

**סניף:** `ChainId`, `SubChainId`, `StoreId`, `BikoretNo` — מפתח הסניף הוא צירוף של אלה, לא `StoreId` לבדו.

**תוקף מבצע:** `PromotionStartDate` + `PromotionStartHour`, `PromotionEndDate` + `PromotionEndHour`. יש לשמור אזור זמן Asia/Jerusalem ולסנן מבצעים שפג תוקפם בזמן ההצגה, לא רק בזמן הייבוא.

**תנאי מועדון:** שדה בסגנון `ClubId` / `IsClubMember`. המוסכמה המקובלת (לאימות) היא ש-`0` מציין מבצע לכלל הלקוחות ומספרים אחרים מציינים מועדון ספציפי. **דרישת מוצר:** מבצע מועדון חייב להיות מסומן בבירור בממשק ואסור שייכלל בהשוואת "מחיר לכולם" ללא סימון — אחרת ההשוואה מטעה.

**מבנה מבצע:** שדות בסגנון `RewardType`, `DiscountType`, `DiscountRate`, `DiscountedPrice`, `MinQty`, `AllowMultipleDiscounts`. מבצעי "3 ב-X" או "השני ב-50%" דורשים חישוב מחיר אפקטיבי ליחידה — אין להציג את מחיר המבצע כאילו הוא מחיר יחידה.

---

## 5. תכנון עדכון כל 15 דקות

**הבהרה חשובה:** 15 דקות היא תדירות ה*בדיקה* שלנו, לא תדירות הפרסום של הרשתות. הרשתות מפרסמות בקצב משלהן (קבצים מלאים לרוב יומיים; דלתאות תכופות יותר). קצב הפרסום בפועל לכל רשת **טרם נמדד** — יש למדוד אותו לפני קיבוע התכנון.

### עקרונות

1. **Poll ולא הורדה עיוורת.** כל 15 דקות: קריאת רשימת הקבצים בלבד, השוואה לחותמת הזמן בשם הקובץ/`Last-Modified`, והורדה רק של מה שחדש.
2. **Conditional GET.** שימוש ב-`If-Modified-Since` ו-`ETag`; טיפול נכון ב-304.
3. **מדרג תדירויות:**
   - `Price` / `Promo` (דלתאות) — כל 15 דקות.
   - `PriceFull` / `PromoFull` — פעם ביום, בשעה נמוכת עומס (למשל 03:00), או כשמזוהה קובץ מלא חדש.
   - `Stores` — פעם ביום.
4. **ריווח בין רשתות.** לא לפנות לחמש הרשתות בו-זמנית; פיזור (jitter) של הבקשות על פני החלון, ובמקביל לכל היותר 2–3 בקשות לאותו host.
5. **Backoff.** על 429/503 — האטה אקספוננציאלית והשהיית הרשת עד המחזור הבא. אין לנסות שוב באגרסיביות; זה מה שהופך שירות לגיטימי לעומס.
6. **User-Agent מזהה.** לציין שם פרויקט ודרך יצירת קשר. אין להתחזות לדפדפן כדי לעקוף סינון.
7. **אחסון גולמי.** לשמור את הקובץ המקורי (`.gz`) לצד הנתונים המעובדים — מאפשר עיבוד מחדש בלי הורדה חוזרת, וזה גם התיעוד שהמחיר לא הומצא.
8. **Idempotency.** מפתח ייחוס `(chain, store, item, file_timestamp)` כדי שהרצה כפולה לא תיצור כפילויות.
9. **חותמת מקור בכל שורה.** לכל מחיר שנשמר: שם הקובץ ושעת הפרסום. בממשק יש להציג "עודכן לאחרונה" — מחיר בלי חותמת זמן הוא מחיר לא אמין.

### מה לא לעשות
אין לעקוף CAPTCHA, אין ליצור חשבונות, ואין להוריד דרך מקורות צד-שלישי שממחזרים את הנתונים אם המקור הרשמי זמין.

---

## 6. חסמים פתוחים

1. **קרפור** — לא אותר עבורה מקור רשמי כלל. זה הפער הגדול ביותר.
2. **ויקטורי (`matrixcatalog.co.il`)** — לא נענה. לא ידוע אם מדובר בכתובת שגויה, ב-HTTPS בלבד, בסאב-דומיין אחר או בתקלה זמנית.
3. **`url.retail.publishedprices.co.il`** — כשל חיבור, בעוד `url.publishedprices.co.il` עונה 200. לא ברור איזו משתיהן היא הכתובת הנוכחית.
4. **gov.il מחזיר 403** ל-curl. העמוד הרשמי המרכז את קישורי הרשתות הוא מקור האמת הטוב ביותר לאימות §3 — ויש להגיע אליו מדפדפן.
5. **פרטי גישה ל-Cerberus לא אומתו** לאף רשת.
6. **מבנה הקבצים לא אומת** מול אף קובץ אמיתי.
7. **לא בוצעה הורדת דוגמה.** לא הורד אף קובץ. כל §4 הוא תיאורטי.

## 7. הצעדים הבאים המומלצים

1. לפתוח את עמוד ה-gov.il מדפדפן ולתעד ממנו את רשימת הקישורים הרשמית — זה מסלק בבת אחת את חסמים 1–4.
2. לאתר בכל אחד מחמשת אתרי הרשתות את דף "שקיפות מחירים" ולתעד את פרטי הגישה שהרשת עצמה מפרסמת.
3. להוריד קובץ `PriceFull` בודד ו-`PromoFull` בודד מרשת אחת, ולאמת מולם את שמות השדות ב-§4 — ורק אז לכתוב parser.
4. למדוד קצב פרסום אמיתי לאורך 24 שעות לפני קיבוע תכנון ה-15 דקות.

---

## 8. הצהרה

לא נערכו קבצי אפליקציה, לא אותחל פרויקט, ולא הופעל שרת. לא הורדו קבצי נתונים ולא נעשה ניסיון לעקוף חסימה כלשהי. אין במסמך נתוני מחיר או מבצע — כל טענה עובדתית כאן מסומנת כ"אומת" או כ"טרם אומת".
